---
import Header from "../../components/Header.astro";
import Footer from "../../components/Footer.astro";
import "../../styles/global.css";
interface Props {
    title: string;
}

const coverImage = "/WaxCover-big.webp";
const coverAlt =
    "Cover for Wax, an abstract image of rectangles of different sizes and textures on a white background";

const title = "Wax";
const author = "Liam Marshall";
const price = "$39";
const format = "hardcover";

const pubdate = "Jan 21, 2024";
const dims = "6 x 9in";
const pagecount = "76 Pages";
const isbn = "978-1-961343-02-3";

// You will need to fill in the blurbs by hand
---

<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="description" content="At Moon Base Books we publish books with heart, craft, and ambition. Whether poetry, essays, mathematics, or games, if it pulses with life we want to publish it." />
        <meta name="keywords" content="Poetry, Publishing, Literature, Essays" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.ico" />
        <meta name="generator" content={Astro.generator} />
        <title>Books - {title}</title>
    </head>
    <body>
        <Header />

        <div class="product-content">
            <img
                class="cover-image"
                src={coverImage}
                alt={coverAlt}
                width="380"
                height="506"
            />

            <div class="purchase-info">
                <p>
                    <b>{title}</b><br /><b>{author}</b><br />{format}<br />{
                        price
                    }
                </p>
            </div>

            <div class="purchase-button">
                <a href="https://www.lulu.com/shop/liam-marshall/wax/hardcover/product-2mm6vq2.html?q=Wax+Liam+Marshall&page=1&pageSize=4">
                    <button class="button-6" role="button">Purchase</button>
                </a>
            </div>

            <div class="book-info">
                <p>{pubdate} | {dims} | {pagecount} | {isbn}</p>
            </div>

            <div class="description">
                <p class="pub-blurb">
                    "Wax" is a book about death: of the deaths we ignore, the
                    deaths we grieve, and the deaths we invite into our own
                    lives because they promise to protect us. It's a book about
                    the poisonous stories we make reality from and the
                    revolution waiting in the soil below us. "Wax" is a book
                    about lives lived, about bodies built and burned.
                </p>

                <hr />
            </div>
        </div>

        <Footer />
    </body>
</html>

<style>
    .product-content {
        display: grid;
        grid-template-columns: 1fr repeat(4, 95px) 1fr;
        gap: 5px;
        grid-auto-rows: minmax(50px, auto);
    }

    .cover-image {
        grid-column: 2 / 6;
        grid-row: 1;
        outline: 2px solid;
    }

    .purchase-info {
        grid-column: 2 / 4;
        grid-row: 2;
    }

    .purchase-button {
        grid-column: 4 / 6;
        grid-row: 2;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-direction: column;
        font-size: small;
    }

    .button {
        height: 50px;
        width: 150px;
    }

    .book-info {
        grid-column: 2 / 6;
        grid-row: 3;
        font-size: small;
    }

    .description {
        grid-column: 2 / 6;
        grid-row: 4;
    }

    .button-6 {
        align-items: center;
        background-color: #ffffff;
        background-color: black;
        border: 2px solid rgba(0, 0, 0, 0.3);
        border-radius: 0.25rem;
        box-shadow: rgba(0, 0, 0, 0.02) 0 1px 3px 0;
        box-sizing: border-box;
        color: rgba(0, 0, 0, 0.85);
        color: #f0f0f1;
        cursor: pointer;
        display: inline-flex;
        font-family:
            system-ui,
            -apple-system,
            system-ui,
            "Helvetica Neue",
            Helvetica,
            Arial,
            sans-serif;
        font-size: 16px;
        font-weight: 600;
        justify-content: center;
        line-height: 1.25;
        margin: 0;
        min-height: 3rem;
        padding: calc(0.875rem - 1px) calc(1.5rem - 1px);
        position: relative;
        text-decoration: none;
        transition: all 250ms;
        user-select: none;
        -webkit-user-select: none;
        touch-action: manipulation;
        vertical-align: baseline;
        width: auto;
    }

    .button-6:hover {
        background-color: #BBB;
        border-color: rgba(0, 0, 0, 0.15);
        box-shadow: rgba(0, 0, 0, 0.06) 0 2px 4px;
        color: rgba(0, 0, 0, 0.65);
    }

    .button-6:active {
        background-color: #ffffff;
        border-color: rgba(0, 0, 0, 0.15);
        box-shadow: rgba(0, 0, 0, 0.06) 0 2px 4px;
        color: rgba(0, 0, 0, 0.65);
    }
</style>
